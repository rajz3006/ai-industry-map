# Finnhub API key (free tier). Get one at https://finnhub.io/register
# Server-only — never exposed to the client. Required for live quotes, profiles,
# candles and earnings; without it the app still builds and runs, showing
# "Set FINNHUB_API_KEY to enable live data" instead of prices.
FINNHUB_API_KEY=
